{
  "name": "pg-json",
  "version": "1.0.0",
  "description": "Define PostgreSQL queries via JSON and render them on the fly.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "handlebars": "^4.0.10",
    "object-path": "^0.11.4",
    "pg": "^7.2.0"
  },
  "optionalDependencies": {
    "commander": "^2.15.1"
  }
}
